PORT=3003

# Ruta al binario de Brave/Chrome que usa Puppeteer. OPCIONAL: si no se
# setea, se autodetecta Brave/Chrome/Chromium instalado según el SO (Mac,
# Linux o Windows) en src/utils/browserPath.util.js. Solo hace falta setearlo
# a mano si tenés el browser en una ruta no estándar.
# BRAVE_PATH=

# Connection string de MongoDB Atlas. Mismo cluster que ms3-armado; para
# desarrollo/pruebas usar la base "test" (agregar "/test" antes del "?").
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/test?appName=FreeVago

# API key de api-ninjas.com, para resolver el aeropuerto más cercano a una
# ciudad (geocoding + lista de aeropuertos). Pedila al equipo, no la commitees.
API_NINJAS_KEY=
